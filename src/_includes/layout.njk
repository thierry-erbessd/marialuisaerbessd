<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <title>{{ title or "María Luisa Erbessd" }}</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta name="description" content="{{ description or 'Sitio de la Dra. María Luisa Erbessd, médica y autora.' }}">
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&family=Playfair+Display:wght@600&display=swap" rel="stylesheet">
  <style>
    :root {
      --bg: #f8f9fb;
      --text: #1f2d3d;
      --muted: #4a5568;
      --accent: #2d7ff9;
      --surface: #ffffff;
      --border: #e2e8f0;
    }

    * { box-sizing: border-box; }

    body {
      margin: 0;
      font-family: "Inter", system-ui, -apple-system, "Segoe UI", sans-serif;
      background: var(--bg);
      color: var(--text);
      line-height: 1.65;
    }

    a {
      color: var(--accent);
      text-decoration: none;
      font-weight: 600;
    }

    a:hover,
    a:focus-visible {
      text-decoration: underline;
    }

    .site-shell {
      min-height: 100vh;
      display: grid;
      grid-template-rows: auto 1fr auto;
    }

    .site-header {
      background: var(--surface);
      border-bottom: 1px solid var(--border);
      position: sticky;
      top: 0;
      z-index: 10;
    }

    .site-bar {
      display: flex;
      align-items: center;
      justify-content: space-between;
      max-width: 1080px;
      margin: 0 auto;
      padding: 1rem 1.25rem;
      gap: 1rem;
    }

    .nav-toggle {
      display: none;
      align-items: center;
      gap: 0.5rem;
      padding: 0.5rem 0.75rem;
      border-radius: 12px;
      border: 1px solid var(--border);
      background: var(--surface);
      color: var(--text);
      font-weight: 700;
      cursor: pointer;
      transition: all 0.2s ease;
    }

    .nav-toggle:hover,
    .nav-toggle:focus-visible {
      border-color: #d7e3ff;
      box-shadow: 0 4px 12px rgba(45, 127, 249, 0.12);
      outline: none;
    }

    .nav-toggle__icon {
      position: relative;
      width: 18px;
      height: 12px;
      display: inline-block;
    }

    .nav-toggle__icon::before,
    .nav-toggle__icon::after,
    .nav-toggle__icon span {
      content: "";
      position: absolute;
      left: 0;
      width: 100%;
      height: 2px;
      background: currentColor;
      border-radius: 999px;
      transition: transform 0.2s ease;
    }

    .nav-toggle__icon::before { top: 0; }
    .nav-toggle__icon span { top: 5px; }
    .nav-toggle__icon::after { top: 10px; }

    .nav-toggle[aria-expanded="true"] .nav-toggle__icon::before {
      transform: translateY(5px) rotate(45deg);
    }

    .nav-toggle[aria-expanded="true"] .nav-toggle__icon span {
      transform: scaleX(0);
    }

    .nav-toggle[aria-expanded="true"] .nav-toggle__icon::after {
      transform: translateY(-5px) rotate(-45deg);
    }

    .brand {
      display: flex;
      align-items: center;
      gap: 0.5rem;
      font-weight: 700;
      letter-spacing: 0.01em;
      color: var(--text);
    }

    .brand span {
      font-family: "Playfair Display", "Inter", serif;
      font-size: 1.1rem;
    }

    .primary-nav {
      display: flex;
      gap: 0.75rem;
      flex-wrap: wrap;
    }

    .primary-nav a {
      color: var(--text);
      padding: 0.35rem 0.75rem;
      border-radius: 999px;
      border: 1px solid transparent;
      transition: all 0.2s ease;
    }

    .primary-nav a:hover,
    .primary-nav a:focus-visible {
      background: #f0f4ff;
      border-color: #d7e3ff;
    }

    .primary-nav.is-open {
      display: flex;
    }

    main {
      max-width: 1080px;
      width: 100%;
      margin: 0 auto;
      padding: 2.5rem 1.25rem 3rem;
    }

    .content-card {
      background: var(--surface);
      border: 1px solid var(--border);
      border-radius: 18px;
      padding: 1.75rem;
      box-shadow: 0 10px 30px rgba(31, 45, 61, 0.06);
    }

    .site-footer {
      background: var(--surface);
      border-top: 1px solid var(--border);
      padding: 1.5rem 1.25rem;
    }

    .footer-content {
      max-width: 1080px;
      margin: 0 auto;
      display: grid;
      gap: 0.35rem;
      color: var(--muted);
      font-size: 0.95rem;
    }

    .footer-contact {
      display: flex;
      gap: 1rem;
      flex-wrap: wrap;
    }

    .tag {
      display: inline-flex;
      align-items: center;
      gap: 0.35rem;
      background: #eef2f7;
      color: var(--muted);
      border-radius: 999px;
      padding: 0.3rem 0.75rem;
      font-size: 0.9rem;
      border: 1px solid #e5e7eb;
    }

    .eyebrow {
      text-transform: uppercase;
      letter-spacing: 0.08em;
      font-weight: 700;
      color: #2d7ff9;
      margin: 0 0 0.35rem;
      font-size: 0.85rem;
    }

    .sr-only {
      position: absolute;
      width: 1px;
      height: 1px;
      padding: 0;
      margin: -1px;
      overflow: hidden;
      clip: rect(0, 0, 0, 0);
      white-space: nowrap;
      border: 0;
    }

    @media (max-width: 720px) {
      .site-bar {
        flex-wrap: wrap;
        align-items: flex-start;
        gap: 0.75rem;
      }

      .nav-toggle {
        display: inline-flex;
      }

      .primary-nav {
        width: 100%;
        display: none;
        flex-direction: column;
        align-items: stretch;
        gap: 0.25rem;
      }

      .primary-nav a {
        width: 100%;
        border-color: var(--border);
        background: #f7f9fc;
        border-radius: 12px;
      }

      .primary-nav a:hover,
      .primary-nav a:focus-visible {
        background: #eef4ff;
      }
    }
  </style>
  <noscript>
    <style>
      @media (max-width: 720px) {
        .primary-nav {
          display: flex !important;
        }
      }
    </style>
  </noscript>
</head>
<body>
  <a class="sr-only" href="#contenido-principal">Saltar al contenido principal</a>
  <div class="site-shell">
    <header class="site-header">
      <div class="site-bar">
        <a class="brand" href="/">
          <span aria-label="Inicio">María Luisa Erbessd</span>
        </a>
        <button class="nav-toggle" type="button" aria-expanded="false" aria-controls="site-nav" data-nav-toggle>
          <span class="nav-toggle__icon" aria-hidden="true"><span></span></span>
          <span class="nav-toggle__label">Menú</span>
        </button>
        <nav id="site-nav" class="primary-nav" aria-label="Navegación principal" data-primary-nav>
          <a href="/">Inicio</a>
          <a href="/biografia/">Biografía</a>
          <a href="/medicina/">Medicina</a>
          <a href="/obra-literaria/">Obra Literaria</a>
          <a href="/contacto/">Contacto</a>
        </nav>
      </div>
    </header>

    <main id="contenido-principal">
      {{ content | safe }}
    </main>

    <footer class="site-footer">
      <div class="footer-content">
        <div class="footer-contact">
          <span class="tag">contacto@ejemplo.com</span>
          <span class="tag">+52 (55) 0000 0000</span>
          <span class="tag">Ciudad de México</span>
        </div>
        <div>&copy; 2025 María Luisa Erbessd. Todos los derechos reservados.</div>
      </div>
    </footer>
  </div>
  <script>
    const navToggle = document.querySelector("[data-nav-toggle]");
    const nav = document.querySelector("[data-primary-nav]");

    if (navToggle && nav) {
      navToggle.addEventListener("click", () => {
        const isOpen = navToggle.getAttribute("aria-expanded") === "true";
        navToggle.setAttribute("aria-expanded", String(!isOpen));
        nav.classList.toggle("is-open", !isOpen);
        const label = navToggle.querySelector(".nav-toggle__label");
        if (label) {
          label.textContent = !isOpen ? "Cerrar menú" : "Menú";
        }
      });
    }
  </script>
</body>
</html>
